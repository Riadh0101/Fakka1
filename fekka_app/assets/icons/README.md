# App Icons

Replace these with actual app icon PNG files (1024x1024):

- `app_icon.png` — Main app icon (1024x1024px)
- `app_icon_foreground.png` — Adaptive icon foreground (1024x1024px, transparent background)

After replacing, run:
```bash
flutter pub run flutter_launcher_icons
```
